package cooba.IndustryPerformance.database.mapper;

import cooba.IndustryPerformance.database.entity.TimeCounter.TimeCounter;

public interface TimeCounterMapper {
    void insert(TimeCounter timeCounter);
}
