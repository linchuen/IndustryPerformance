<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace 對應持久層的介面 -->
<mapper namespace="cooba.IndustryPerformance.database.mapper.TimeCounterMapper">
    <insert id="insert">
        INSERT INTO
        time_counter(id, uuid, method, message, total_seconds, create_time)
        VALUES
        (#{id}, #{uuid}, #{method}, #{message}, #{totalSeconds}, #{createTime})
    </insert>
</mapper>